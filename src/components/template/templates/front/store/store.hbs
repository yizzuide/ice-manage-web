import usePiniaStore from "@/hooks/usePiniaStore";
import { HttpResult } from "@/http/HttpDefine";
import request from "@/http/UniformRequest";

// 实体对象
export {{entity}}

// 定义扩展的Action
export interface {{moduleName}}Action {

}

export const usePageStore = usePiniaStore<{{moduleName}}>({
  name: "{{camelCase moduleName}}",
  actions: {

  }
});
